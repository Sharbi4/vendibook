# Vendibook Environment Variables
# Copy this file to .env.local and fill in with your actual values

# ============================================================================
# DATABASE
# ============================================================================

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Examples:
#   Local: postgresql://postgres:password@localhost:5432/vendibook_dev
#   Supabase: postgresql://postgres.xxxxx:password@db.xxxxx.supabase.co:5432/postgres
#   Railway: postgresql://user:password@rail.proxy.rlwy.net:port/railway
DATABASE_URL="postgresql://postgres:password@localhost:5432/vendibook_dev"

# ============================================================================
# AUTHENTICATION
# ============================================================================

# JWT Secret Key (use a strong random string in production)
# Generate: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT Token Expiration
# Format: "7d" (7 days), "24h" (24 hours), "60" (60 seconds), etc.
JWT_EXPIRY="7d"

# ============================================================================
# AWS S3 (Image Storage)
# ============================================================================

# AWS Region (e.g., us-east-1, us-west-2, eu-west-1)
AWS_REGION="us-east-1"

# AWS Access Key ID
# Get from: AWS Console → IAM → Users → Security Credentials
AWS_ACCESS_KEY_ID="your-aws-access-key-id"

# AWS Secret Access Key
# Get from: AWS Console → IAM → Users → Security Credentials
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"

# S3 Bucket Name (must already exist)
# Create: AWS Console → S3 → Create Bucket
AWS_S3_BUCKET="vendibook-images"

# S3 Public URL (for serving images)
# Default: https://{bucket}.s3.{region}.amazonaws.com
# CloudFront: https://d123456.cloudfront.net
AWS_S3_URL="https://vendibook-images.s3.us-east-1.amazonaws.com"

# ============================================================================
# FRONTEND
# ============================================================================

# API Base URL (for frontend to call backend)
# Development: http://localhost:3000
# Production: https://api.vendibook.com
REACT_APP_API_URL="http://localhost:3000"

# ============================================================================
# EMAIL (for future notifications)
# ============================================================================

# SendGrid API Key (for sending emails)
# Get from: SendGrid Dashboard → Settings → API Keys
SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Email from address
SENDGRID_FROM_EMAIL="noreply@vendibook.com"

# ============================================================================
# OPTIONAL: STRIPE (for future payments)
# ============================================================================

# Stripe Public Key (publishable key)
STRIPE_PUBLIC_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxx"

# Stripe Secret Key (secret key)
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxxx"

# ============================================================================
# LOGGING & MONITORING
# ============================================================================

# Log Level: error, warn, info, debug, trace
LOG_LEVEL="info"

# Sentry Error Tracking (optional)
SENTRY_DSN="https://xxxxxxxxxxxxx@xxxxx.ingest.sentry.io/xxxxx"

# ============================================================================
# DEVELOPMENT
# ============================================================================

# Node Environment
NODE_ENV="development"

# Enable debug logging
DEBUG="vendibook:*"

# ============================================================================
# NOTES
# ============================================================================
#
# SECURITY:
# - NEVER commit .env.local to git
# - Use strong random strings for secrets
# - Rotate secrets regularly in production
# - Use environment-specific secrets (dev vs staging vs prod)
#
# DATABASES:
# Local PostgreSQL:
#   brew install postgresql@14
#   brew services start postgresql@14
#   createdb vendibook_dev
#   psql vendibook_dev -c "SELECT 1"
#
# Managed Services:
#   - Supabase (https://supabase.com) - PostgreSQL as a service
#   - Railway (https://railway.app) - Host database and API
#   - Render (https://render.com) - Simple PostgreSQL hosting
#   - PlanetScale (https://planetscale.com) - MySQL alternative
#
# AWS S3:
# 1. Create S3 bucket: https://s3.console.aws.amazon.com
# 2. Create IAM user with S3 permissions
# 3. Get access key and secret key
# 4. Bucket policy for public read:
#    {
#      "Version": "2012-10-17",
#      "Statement": [{
#        "Effect": "Allow",
#        "Principal": "*",
#        "Action": "s3:GetObject",
#        "Resource": "arn:aws:s3:::bucket-name/*"
#      }]
#    }
#
# DEPLOYMENT:
# - Vercel: Add env vars in Vercel dashboard
# - Railway: Add vars in Railway dashboard
# - Render: Add vars in Render dashboard
# - Docker: Use docker run -e VAR=value
#
